spring:
  jpa:
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
      encoding: utf-8

logging.level:
  louie:
    hanse:
      shareplate:
        oauth:
          client: debug

feign:
  client:
    config:
      default:
        loggerLevel: FULL
