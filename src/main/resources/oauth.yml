jwt:
  issuer: ${JWT_ISSUER}
  secret-key: ${JWT_SECRET_KEY}

oauth:
  redirect_url: ${OAUTH_REDIRECT_URL}
  client_id: ${OAUTH_CLIENT_ID}
  token:
    api_url: ${OAUTH_TOKEN_API_URL}
  login_form_url: ${OAUTH_LOGIN_FORM_URL}
  user:
    api_url: ${OAUTH_USER_API_URL}
